// The function is O(n) - an O-of-n operation.
// We pass the parameter n and the operation ran n-times.

function logItems(n) {
    for(let i = 0; i < n; i++) {
        console.log(i)
    }
}

logItems(10)